# Source Directory

This folder is reserved for reusable source code modules and package components.

Current repository status: source modules are not yet extracted to `src/`, but this directory exists to support a professional Python package layout.
